# Домен Идентификации (Identity Domain)

## Модели

### User (Пользователь)
Локальный пользователь платформы aka "Клиент".
* **id** (uuid): Внутренний ID пользователя в Mentor.
* **networkUserId** (string): Внешний ключ, ID пользователя в Network (SSO). Используется для маппинга.
* **settings** (object): Глобальные настройки.
    * `interfaceLanguage` (string): Код языка (en, ru), по умолчанию en. Используется для общения с ботами.
    * *Примечание*: UI-настройки (тема, анимации) хранятся локально на клиенте и не синхронизируются.

### UserProfile (RT View)
Агрегированный профиль, собираемый "на лету" из Network и локальных настроек.
* **user**: Локальные данные (`User`).
* **networkData**: Данные из внешнего сервиса (Real-time).
    * `username` (string).
    * `avatarUrl` (string).
    * `balances` (list BalanceItem): Мультивалютный кошелек.
        * `amount` (string): Значение ("10.500").
        * `currencyId` (u32): ID валюты.
    * `depositUrl` (string).

### Currency (Валюта)
Справочник валют и ассетов.
* **id** (u32): Уникальный ID.
* **code** (string): Уникальный код (TON, USDT, POINTS).
* **displayName** (string): Название.
* **symbol** (string): Символ ($, 💎).
* **decimals** (int): Точность.

### ExchangePair (Валютная пара)
Курс обмена для отображения цен.
* **baseCurrency** (string): Что продаем (например, USD - цена тарифа).
* **quoteCurrency** (string): В чем платим (например, TON).
* **rate** (decimal): Курс (сколько TON даем за 1 USD).

### SystemConfig (Global)
Справочники системы.
* **supportedLocales** (list object):
    * `code` (string): 'en', 'ru'.
    * `displayName` (string): 'English', 'Русский'.
